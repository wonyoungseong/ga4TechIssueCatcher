# Local Supabase Environment Configuration
# This file is used by docker-compose.yml for local development
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local: cp .env.local.example .env.local
# 2. Replace placeholder values with actual credentials
# 3. For local development, you can use the demo JWT keys provided
# 4. For production, generate secure random values

# ============================================
# Database Configuration
# ============================================
POSTGRES_PASSWORD=your-super-secret-password

# ============================================
# JWT Configuration
# ============================================
# IMPORTANT: This must be at least 32 characters long
# Generate a secure random string for production use
# Example command: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-token-with-at-least-32-characters

# ============================================
# Supabase API Keys
# ============================================
# Anon Key - Public key for client-side access (limited permissions)
# For local dev, you can use Supabase demo key:
# eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
ANON_KEY=your-anon-key-here

# Service Role Key - Admin key with full permissions (keep secret!)
# For local dev, you can use Supabase demo key:
# eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU
SERVICE_KEY=your-service-role-key-here

# ============================================
# Local Supabase URLs
# ============================================
# These will be used by the application to connect to local Supabase
SUPABASE_URL=http://localhost:8000
SUPABASE_ANON_KEY=${ANON_KEY}
SUPABASE_SERVICE_ROLE_KEY=${SERVICE_KEY}

# ============================================
# Service Endpoints
# ============================================
# Studio (Admin Dashboard)
STUDIO_URL=http://localhost:3001

# PostgREST API
POSTGREST_URL=http://localhost:3000

# Storage API
STORAGE_URL=http://localhost:5000

# Realtime
REALTIME_URL=http://localhost:4000

# Auth
AUTH_URL=http://localhost:9999

# ============================================
# Notes
# ============================================
# 1. For local development, the demo JWT keys provided in comments work out of the box
# 2. For production, generate your own secure JWT_SECRET and derive keys from it
# 3. All services are accessible via Kong API Gateway at http://localhost:8000
# 4. Supabase Studio is available at http://localhost:3001
# 5. NEVER commit .env.local to version control - it contains secrets!
