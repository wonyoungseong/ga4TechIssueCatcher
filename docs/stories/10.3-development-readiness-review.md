# Story 10.3 개발 가능성 검수 보고서

**검수일**: 2025-11-07
**Story**: 10.3 - Network Error Retry Queue System
**검수자**: Development Review
**결과**: ✅ **개발 가능 (수정 필요 사항 있음)**

---

## 📋 Executive Summary

Story 10.3은 **개발 가능**하지만, 몇 가지 **중요한 수정**이 필요합니다.

**종합 점수**: 8.5/10

**주요 이슈**:
1. ❌ **중대한 오류**: 존재하지 않는 `validator.js` 모듈 참조
2. ⚠️ **함수 시그니처 불일치**: `validateSingleProperty` 파라미터 순서 변경 필요
3. ⚠️ **Import 누락**: Supabase import 문 추가 필요
4. ℹ️ **기타**: 기존 구조와의 통합 방식 조정 필요

---

## 1. 코드베이스 호환성 검증

### ✅ 통과 항목

#### 1.1 데이터베이스 스키마
- ✅ Supabase PostgreSQL 사용 중 (호환)
- ✅ UUID 기본 타입 지원
- ✅ Foreign Key 제약 조건 사용 가능
- ✅ Timestamp 타입 지원
- ✅ Status ENUM 제약 조건 사용 가능

#### 1.2 의존성
- ✅ Express.js 이미 설치됨
- ✅ Playwright 이미 사용 중
- ✅ Supabase 클라이언트 설정됨 (`src/utils/supabase.js`)
- ✅ 추가 외부 라이브러리 불필요

#### 1.3 아키텍처
- ✅ Phase 1/Phase 2 시스템 동작 중
- ✅ Browser Pool 구조 존재
- ✅ WebSocket 실시간 업데이트 시스템 존재
- ✅ 라우팅 구조 (`src/routes/`) 확립됨

### ❌ 수정 필요 항목

#### 2.1 🚨 중대한 오류: validator.js 모듈 존재하지 않음

**Story 내용** (src/modules/retryQueue.js):
```javascript
import { validateSingleProperty } from './validator.js';
```

**실제 상황**:
```bash
❌ /src/modules/validator.js 파일이 존재하지 않음
✅ validateSingleProperty 함수는 orchestrator.js 내에 정의됨
```

**수정 방안**:
```javascript
// BEFORE (Story에서 제시한 잘못된 import)
import { validateSingleProperty } from './validator.js';

// AFTER (올바른 import)
import { validateSingleProperty } from './orchestrator.js';
```

**파일 위치**: `/Users/seong-won-yeong/Dev/ga4TechIssueCatcher/src/modules/orchestrator.js:1537`

#### 2.2 ⚠️ 함수 시그니처 불일치

**Story에서 제시한 호출**:
```javascript
const result = await validateSingleProperty(
  browser,
  retry.properties,
  dateStr,
  'retry',  // ❌ 4번째 파라미터가 문자열
  80000     // 5번째 파라미터
);
```

**실제 함수 시그니처** (orchestrator.js:1537):
```javascript
async function validateSingleProperty(
  browser,
  property,
  dateStr,
  phase = 1,                    // 4번째: phase 번호 (숫자)
  timedOutPropertyIds = null,   // 5번째: Set
  timeoutMs = null              // 6번째: timeout 값
)
```

**수정 필요**:
```javascript
// Story 수정 필요 (retryQueue.js)
const result = await validateSingleProperty(
  browser,
  retry.properties,
  dateStr,
  2,                // Phase 2 (숫자로)
  new Set(),        // timedOutPropertyIds (빈 Set)
  80000             // timeout (6번째 파라미터로)
);
```

#### 2.3 ⚠️ Import 누락

**Story의 retry.js**에서 Supabase import 누락:
```javascript
// 현재 Story 내용
import express from 'express';
import playwright from 'playwright';
import { processRetryQueue } from '../modules/retryQueue.js';

// 수정 필요
import express from 'express';
import playwright from 'playwright';
import supabase from '../utils/supabase.js';  // ✅ 추가 필요
import { processRetryQueue } from '../modules/retryQueue.js';
```

**이유**: `/retry-queue/status` 엔드포인트에서 `supabase` 사용

#### 2.4 Phase 2 실패 처리 위치 확인

**Story에서 제시한 위치**:
```javascript
// src/modules/orchestrator.js - Phase 2 failure handling (line ~450)
```

**실제 위치**:
```
✅ Line 1040-1100: Phase 2 에러 처리 로직
✅ Line 1069-1087: errorResult 생성 및 캐싱
```

**권장 삽입 위치**:
```javascript
// orchestrator.js:1087 이후 추가
// Phase 2 error result cache storage 이후
// Retry queue 삽입 로직 추가
```

---

## 2. 구현 가능성 평가

### Phase 1: Database & Core Logic ✅

**Task 1.1: retry_queue 테이블 생성**
- ✅ Supabase Dashboard에서 SQL 실행 가능
- ✅ 제시된 마이그레이션 스크립트 문법 정확
- ✅ Index 전략 적절
- ✅ Trigger 함수 PostgreSQL 호환

**예상 소요 시간**: 30분

**Task 1.2: Retry queue 삽입 (orchestrator.js)**
- ✅ 삽입 위치 확인됨 (line 1087 이후)
- ⚠️ Import 추가 필요 없음 (이미 supabase import 존재)
- ✅ Property 객체 구조 호환
- ✅ runId 변수 접근 가능

**수정된 코드 예시**:
```javascript
// orchestrator.js:1087 이후 추가
// Phase 2 failure → Add to retry queue
if (errorResult.validationStatus === 'failed' ||
    errorResult.validationStatus === 'timeout') {

  try {
    await supabase
      .from('retry_queue')
      .insert({
        property_id: property._supabaseId,
        crawl_run_id: runId,
        failure_reason: errorResult.issueSummary,
        next_retry_at: new Date(Date.now() + 30 * 60 * 1000)
      });

    console.log(`  📋 Added to retry queue: ${property.propertyName}`);
  } catch (queueError) {
    console.error(`  ⚠️ Failed to add to retry queue: ${queueError.message}`);
  }
}
```

**예상 소요 시간**: 1시간

**Task 1.3: retryQueue.js 모듈 생성**
- ⚠️ **수정 필요**: Import 경로 변경
- ✅ 로직 구조 양호
- ✅ Exponential backoff 계산 정확
- ⚠️ **수정 필요**: `validateSingleProperty` 호출 파라미터 순서

**수정된 import**:
```javascript
// src/modules/retryQueue.js
import supabase from '../utils/supabase.js';
import { validateSingleProperty } from './orchestrator.js';  // ✅ 수정
```

**수정된 함수 호출**:
```javascript
const result = await validateSingleProperty(
  browser,
  retry.properties,
  dateStr,
  2,          // Phase 2 (retry)
  new Set(),  // timedOutPropertyIds
  80000       // timeout
);
```

**예상 소요 시간**: 1.5시간

---

### Phase 2: Automation & API ✅

**Task 2.1: Automatic Scheduler (server.js)**
- ✅ 서버 구조 확인됨
- ✅ setInterval 사용 가능
- ⚠️ **권장 사항**: `recoverIncompleteCrawls` 이후 추가 (server.js:26 참조)

**삽입 위치**:
```javascript
// src/server.js
// Line ~100-120 (서버 시작 이후, 첫 크롤 이전)

// After server setup, before crawl start
import { processRetryQueue } from './modules/retryQueue.js';
import playwright from 'playwright';

// Retry Queue Scheduler (30 minutes)
setInterval(async () => {
  try {
    console.log('\n🔄 [Retry Queue] Scheduled processing started');

    const browser = await playwright.chromium.launch({ headless: true });
    const dateStr = new Date().toISOString().split('T')[0];

    const stats = await processRetryQueue(browser, dateStr);

    await browser.close();

    console.log(`✅ [Retry Queue] Completed: ${stats.succeeded} succeeded, ${stats.failed} failed`);
  } catch (error) {
    console.error('❌ [Retry Queue] Scheduler error:', error.message);
  }
}, 30 * 60 * 1000);

console.log('✅ Retry Queue scheduler initialized (30-minute intervals)');
```

**예상 소요 시간**: 30분

**Task 2.2: retry.js 라우트 생성**
- ✅ 라우팅 구조 확인됨 (`src/routes/`)
- ⚠️ **수정 필요**: Supabase import 추가
- ✅ API 구조 양호

**server.js에 라우트 등록** (server.js:23 참조):
```javascript
// src/server.js
import retryRouter from './routes/retry.js';  // ✅ 추가

// ...

app.use('/api', retryRouter);  // ✅ 등록
```

**예상 소요 시간**: 1시간

---

### Phase 3: Frontend UI ⚠️

**Task 3.1-3.3: React 컴포넌트 생성**
- ✅ React 프로젝트 구조 확인됨 (`front/crawler-monitor/`)
- ⚠️ **검증 필요**: 기존 컴포넌트 구조 파악 필요
- ⚠️ **검증 필요**: API 호출 패턴 통일 필요

**권장 사항**:
1. 기존 `front/crawler-monitor/src/` 구조 분석
2. API client 패턴 확인
3. 컴포넌트 스타일 가이드 준수

**예상 소요 시간**: 3-4시간 (Story 예상치 유지)

---

### Phase 4: Optimization (Optional) ✅

**Task 4.1: Network Error Detection**
- ✅ `isRetryableError` 함수 이미 존재 (orchestrator.js:155)
- ✅ 네트워크 오류 키워드 이미 정의됨
- ✅ 통합 가능

**예상 소요 시간**: 1시간

---

## 3. 위험 요소 평가

### 🔴 High Risk

**없음** (모든 critical issue는 수정 가능)

### 🟡 Medium Risk

1. **Browser Pool 자원 경쟁**
   - **Risk**: Scheduler와 Manual Retry 동시 실행 시 browser pool 고갈
   - **완화 방안**: Browser pool size 확인 및 동적 조정 로직 추가
   - **영향도**: Medium

2. **Database 부하**
   - **Risk**: Retry queue 조회가 빈번할 경우 성능 저하
   - **완화 방안**: Index 최적화 (Story에 이미 포함됨)
   - **영향도**: Low-Medium

### 🟢 Low Risk

1. **Scheduler 메모리 누수**
   - **Risk**: setInterval이 제대로 정리되지 않을 경우
   - **완화 방안**: 서버 종료 시 clearInterval 추가
   - **영향도**: Low

---

## 4. 테스트 전략 평가

### Unit Tests ✅
- ✅ 테스트 케이스 구체적
- ✅ Jest 프레임워크 사용 가능
- ✅ Supabase mocking 가능

### Integration Tests ✅
- ✅ API 테스트 가능
- ✅ Scheduler 테스트 가능
- ⚠️ **권장**: Transaction rollback 테스트 추가

### E2E Tests ✅
- ✅ 시나리오 현실적
- ✅ Playwright 사용 가능
- ⚠️ **권장**: 30분 대기 시간 Mock 필요 (실제 30분 대기는 비현실적)

---

## 5. 예상 Timeline 수정

| Phase | Story 예상 | 실제 예상 | 차이 | 비고 |
|-------|---------|---------|-----|-----|
| Phase 1 | 2-3h | 3h | +0-1h | 함수 시그니처 수정 시간 추가 |
| Phase 2 | 2-3h | 2h | -1h | 구조 단순 |
| Phase 3 | 3-4h | 4-5h | +1h | 기존 코드 분석 시간 |
| Phase 4 | 2-3h | 1-2h | -1h | 기존 함수 재사용 |
| **총계** | **14-16h** | **14-17h** | **±1h** | **예상치 유지** |

---

## 6. 수정 요구 사항 정리

### 필수 수정 (Critical) ❗

#### 수정 1: retryQueue.js Import
**파일**: Story 10.3 - Component 2 (retryQueue.js)

```javascript
// BEFORE
import { validateSingleProperty } from './validator.js';

// AFTER
import { validateSingleProperty } from './orchestrator.js';
```

#### 수정 2: validateSingleProperty 호출
**파일**: Story 10.3 - Component 2 (retryQueue.js:163-169)

```javascript
// BEFORE
const result = await validateSingleProperty(
  browser,
  retry.properties,
  dateStr,
  'retry',  // ❌ Wrong type
  80000
);

// AFTER
const result = await validateSingleProperty(
  browser,
  retry.properties,
  dateStr,
  2,          // ✅ Phase 2 (number)
  new Set(),  // ✅ timedOutPropertyIds
  80000       // ✅ timeout (6th parameter)
);
```

#### 수정 3: retry.js Import
**파일**: Story 10.3 - Component 4 (retry.js)

```javascript
// BEFORE
import express from 'express';
import playwright from 'playwright';
import { processRetryQueue } from '../modules/retryQueue.js';

// AFTER
import express from 'express';
import playwright from 'playwright';
import supabase from '../utils/supabase.js';  // ✅ 추가
import { processRetryQueue } from '../modules/retryQueue.js';
```

#### 수정 4: orchestrator.js 삽입 위치 명확화
**파일**: Story 10.3 - Dev Notes

```markdown
// BEFORE
1. **src/modules/orchestrator.js** (~line 450)

// AFTER
1. **src/modules/orchestrator.js** (~line 1087)
   - Phase 2 error result cache storage 이후
   - errorResult.validationStatus 확인 가능 지점
```

### 권장 수정 (Recommended) ⚠️

#### 권장 1: Browser Pool 체크
**파일**: retryQueue.js

```javascript
export async function processRetryQueue(browser, dateStr) {
  // Add browser pool availability check
  const availableBrowsers = await browserPool.getAvailableCount();

  if (availableBrowsers < 1) {
    console.warn('⚠️ No available browsers - deferring retry queue processing');
    return { processed: 0, succeeded: 0, failed: 0, deferred: true };
  }

  // ... existing logic
}
```

#### 권장 2: Environment Variables
**파일**: `.env`

```bash
# Retry Queue Configuration
RETRY_QUEUE_INTERVAL_MINUTES=30
RETRY_QUEUE_MAX_ATTEMPTS=3
RETRY_QUEUE_INITIAL_DELAY=30
RETRY_QUEUE_BATCH_SIZE=50
```

---

## 7. 최종 의견

### ✅ 개발 가능 여부: **가능**

**근거**:
1. 모든 critical issue는 **간단한 수정**으로 해결 가능
2. 아키텍처 충돌 없음
3. 의존성 문제 없음
4. 예상 timeline 현실적

### 🎯 권장 실행 순서

1. **Story 10.3 문서 수정** (30분)
   - Import 경로 수정
   - 함수 호출 파라미터 수정
   - 위치 명확화

2. **Phase 1 구현** (3h)
   - Database 테이블 생성
   - orchestrator.js 수정
   - retryQueue.js 생성

3. **Phase 1 테스트** (1h)
   - Unit test 작성
   - 수동 실패 케이스 생성
   - Retry queue 삽입 확인

4. **Phase 2 구현** (2h)
   - Scheduler 추가
   - API 엔드포인트 생성

5. **Phase 2 테스트** (1h)
   - API 테스트
   - Scheduler 동작 확인

6. **Phase 3 구현** (4-5h)
   - Frontend 컴포넌트
   - UI 통합

7. **Integration Test** (2h)
   - E2E 시나리오 테스트
   - 성능 검증

**총 예상 시간**: **13.5-15.5 hours** (Story 예상치보다 약간 단축)

### 📊 최종 점수

| 항목 | 점수 | 비고 |
|-----|------|-----|
| 코드 호환성 | 9/10 | Import 경로만 수정 |
| 아키텍처 통합 | 9/10 | 기존 구조와 잘 맞음 |
| 구현 가능성 | 9/10 | 모든 리소스 확보 |
| 테스트 용이성 | 8/10 | 30분 대기 Mock 필요 |
| 리스크 수준 | 8/10 | Medium risk만 존재 |
| 문서 정확성 | 7/10 | 수정 필요 사항 있음 |
| **총점** | **8.3/10** | **개발 가능** |

---

## 8. Action Items

### 즉시 실행 (Immediate)
- [ ] Story 10.3 문서 수정 (4개 critical fix)
- [ ] Database migration script 준비
- [ ] Development branch 생성

### 개발 전 (Before Development)
- [ ] Frontend 구조 분석 (기존 컴포넌트 패턴 파악)
- [ ] Browser pool 현황 확인
- [ ] Test DB 환경 준비

### 개발 중 (During Development)
- [ ] 각 Phase 완료 후 테스트
- [ ] 성능 모니터링 (특히 Scheduler)
- [ ] 로그 레벨 조정

### 배포 전 (Before Deployment)
- [ ] Staging 환경 테스트
- [ ] Database index 성능 검증
- [ ] 롤백 계획 수립

---

**결론**: Story 10.3은 명시된 수정사항을 반영한 후 **즉시 개발 가능**합니다.

**승인**: ✅ **Approved for Development** (수정 조건부)
